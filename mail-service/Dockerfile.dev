FROM node:12.16.3-alpine

ARG NODE_ENV
ARG MAILGUN_HOST
ARG MAILGUN_API_KEY
ARG MAILGUN_DOMAIN
ARG DOMAIN_NAME
ARG PORT
ARG REDIS_PORT
ARG REDIS_HOST

WORKDIR /usr/src/app
COPY package.json yarn.lock ./
RUN yarn
COPY . ./

ENTRYPOINT [ "yarn", "start:dev" ]